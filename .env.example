# Open Exchange Rates — API Key (app_id)
# Crea una cuenta gratuita en https://openexchangerates.org y copia tu App ID aquí.
# Copia este archivo a `.env.local` y completa el valor. NUNCA commitees el valor real.
open_exchange_app_id=
