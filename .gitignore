# Secrets — nunca commitear la API key de Open Exchange Rates
.env
.env.local
.env.*.local

# Entornos virtuales
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# Type checker
.pyright/

# Archivos de Claude
# Las skills (.claude/skills/) y agentes (.claude/agents/) SÍ se versionan: son trabajo por
# iniciativa propia que mostramos (ver docs/08-extras-iniciativa.md). Lo que queda local:
# - CLAUDE.md: instrucciones internas del proyecto
# - .claude/settings.local.json: ajustes/permisos locales de la herramienta
CLAUDE.md
.claude/settings.local.json

# Guion del video — local, no se sube al repo
GUION_VIDEO.md

# Preparación de la entrevista — local, no se sube al repo
ENTREVISTA_PREP.md

# OS
.DS_Store
